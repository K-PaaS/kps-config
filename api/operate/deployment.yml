#
#
# kps-api-operate deployment.yaml
#
# 
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kps-api-operate
  namespace: kps2
  labels:
    app: kps-api-operate
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: kps-api-operate
  template:
    metadata:
      labels:
        app: kps-api-operate
    spec:
      containers:
      - name: kps-api-operate
        image: kps-container-registry.ncr.gov-ntruss.com/kps-api-operate:1.21-dev
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 250m
            memory: 700Mi
          limits:
            cpu: 500m
            memory: 2000Mi                
        ports:
        - containerPort: 9070
      imagePullSecrets:
        - name: ncp-container
