#
#
# kps-api-operate deployment.yaml
#
# 
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kps-api-operate
  namespace: kps2
  labels:
    app: kps-api-operate
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kps-api-operate
  template:
    metadata:
      labels:
        app: kps-api-operate
    spec:
      containers:
      - name: kps-api-operate
        image: kps-container-registry.ncr.gov-ntruss.com/kps-api-operate:1.8-dev
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 10m
            memory: 100Mi
          limits:
            cpu: 180m
            memory: 2000Mi                
        ports:
        - containerPort: 9070
      imagePullSecrets:
        - name: ncp-container